# Frontend

React + Vite UI for the Nowshera Digital ATS.

Setup, environment variables, and how to run the app are in the [repository README](../README.md).

```bash
npm install
npm run dev
npm run build
npm run lint
```

Use `frontend/.env` for `VITE_*` values. Do not commit that file.
